name: Dev Build

# Manual, on-demand build of a test exe off any branch. Produces the same
# self-contained win-x64 single-file exe as the release workflow but uploads
# it as a build artifact instead of cutting a GitHub Release. The version is
# the same Major.Minor.Build with a -dev suffix.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Compute version
        id: version
        shell: bash
        run: |
          version="$(bash .github/version.sh)-dev"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Version: ${version}"

      - name: Publish single-file self-contained exe (win-x64)
        shell: bash
        run: |
          dotnet publish Flatline.csproj \
            -c Release \
            -r win-x64 \
            --self-contained true \
            -p:PublishSingleFile=true \
            -p:IncludeNativeLibrariesForSelfExtract=true \
            -p:Version="${{ steps.version.outputs.version }}" \
            -o publish

      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p artifacts
          mv publish/Flatline.exe "artifacts/Flatline-${{ steps.version.outputs.version }}-win-x64.exe"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Flatline-dev-${{ github.sha }}
          path: artifacts/Flatline-${{ steps.version.outputs.version }}-win-x64.exe
          retention-days: 14
          if-no-files-found: error
